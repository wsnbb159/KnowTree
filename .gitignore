node_modules/
dist/

# 本机构建环境的临时缓存与归档（受 sandbox 保护，无法删除，只能忽略）
._del_*
._old_dist/
._prev_dist*/
.npm-cache/
.trash/
pdfjs-dist-*.tgz

# 本地配置
*.local
.DS_Store

# Vite 临时文件（每次 dev/build 都会生成新的时间戳副本）
vite.config.ts.timestamp-*.mjs

# 验证截图（QA 过程产物，非源码）
docs/qa/
docs/qa-*.png

# 专家包打包产物（可由 scripts/ 下脚本重新生成，不入源码仓库）
knowtree.zip

# 赛事方提供的材料，不作为源码仓库内容
docs/赛事手册全文.txt
